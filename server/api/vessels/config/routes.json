{
  "routes": [
    {
      "method": "GET",
      "path": "/vessels",
      "handler": "vessels.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vessels/count",
      "handler": "vessels.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vessels/:id",
      "handler": "vessels.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/vessels",
      "handler": "vessels.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/vessels/:id",
      "handler": "vessels.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/vessels/:id",
      "handler": "vessels.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
