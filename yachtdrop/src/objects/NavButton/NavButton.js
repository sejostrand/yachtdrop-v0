import React, { Component } from 'react';
import classes from './NavButton.module.css';

const NavButton = () => {
  return <button className={classes.NavButton1}>Hello</button>;
};

export default NavButton;
